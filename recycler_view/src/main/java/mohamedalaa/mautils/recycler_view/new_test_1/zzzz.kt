package mohamedalaa.mautils.recycler_view.new_test_1

import android.content.Context
import androidx.recyclerview.widget.GridLayoutManager
import androidx.recyclerview.widget.RecyclerView



/*
private class Z1: RecyclerView.LayoutManager() {

    override fun generateDefaultLayoutParams(): RecyclerView.LayoutParams {

    }



}

private class Z2(context: Context, spanCount: Int): GridLayoutManager(context, spanCount) {
    override fun getSpaceForSpanRange(startSpan: Int, spanSize: Int): Int {
        return super.getSpaceForSpanRange(startSpan, spanSize)
    }

    override fun setSpanSizeLookup(spanSizeLookup: SpanSizeLookup?) {
        super.setSpanSizeLookup(spanSizeLookup)
    }

    private fun dijwoedjo(gridLayoutManager: GridLayoutManager) {
        gridLayoutManager.spanSizeLookup = object : SpanSizeLookup {

        }

        val ss: DefaultSpanSizeLookup
    }
}*/

private fun a1(recyclerView: RecyclerView) {
    //recyclerView.itemdeco
}
